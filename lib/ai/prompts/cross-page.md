You are auditing cross-page consistency for the site {{DOMAIN}} using REAL data extracted from {{PAGE_COUNT}} crawled pages.

Per-page data:
{{PAGES}}

Site-level findings already recorded by the rule engine (do not repeat):
{{RULE_RESULTS}}

Look ONLY for cross-page problems:
- inconsistent titles/description patterns across pages
- button/CTA wording that differs where it should match
- navigation or footer structure that differs between pages
- inconsistent heading conventions or content structure between similar pages
- pages that look orphaned or unreachable from the others

Report at most {{MAX_FINDINGS}} findings grounded in the data. If the site is consistent, return fewer or none.

Also write "summary": a 2-4 sentence executive summary of the WHOLE site's health for a business owner: lead with the issues that cost the most money, name the top 2-3 things to fix first, and state the expected benefit. Base it on the real check results and scores below:
{{SCORES_AND_ISSUES}}
